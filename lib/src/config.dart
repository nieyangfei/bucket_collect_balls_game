/* Game configs. */
const debugMode = false;
const gameWidth = 820.0;
const gameHeight = 1600.0;
/* Ball configs. */
const ballRadius = gameWidth * 0.02;
/* Bat configs. */
const bucketWidth = gameWidth * 0.2;
const bucketHeight = ballRadius * 2;
const bucketShiftStep = gameWidth * 0.05;